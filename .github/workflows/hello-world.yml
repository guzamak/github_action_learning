name: Deploy Static Site

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        uses: guzamak/github_action_learning
        with:
          folder: .   # deploy everything in the root (index.html must be here)
