# ชื่อ ci/cd
name : My First Workflow

#
on :
  push :
    # 
    branches:
      - main
# 
jobs :
  build :
    # deploy system
    runs-on : ubuntu-lastest
  
    # ขั้นตอนของ workflows
    steps :
    # เอาโค้ดมาจาก repo
    - name: Checkout 🛎️
        uses: actions/checkout@v4

    - name: Say Hello
    # script to run in linux
      run : echo "Hello, Github Action"
  deploy:
      uses: guzamak/github-pages-deploy-action@v4
      with:
        folder: ./