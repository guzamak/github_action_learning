name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello, Github Action"

  deploy:
    runs-on: ubuntu-latest
    needs: build   # ให้ deploy หลังจาก build เสร็จ

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        uses: guzamak/github-pages-deploy-action@v4
        with:
          folder: .
